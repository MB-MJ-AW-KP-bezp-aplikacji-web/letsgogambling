version: '3.8'

services:
  db:
    image: postgres:16-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: casino
      POSTGRES_USER: casino_user
      POSTGRES_PASSWORD: casino_password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U casino_user -d casino"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    command: sh -c "python manage.py migrate && daphne -b 0.0.0.0 -p 8000 casino.asgi:application"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DJANGO_DEBUG: false
      DB_NAME: casino
      DB_USER: casino_user
      DB_PASSWORD: casino_password
      DB_HOST: db
      DB_PORT: 5432
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: on-failure

  roulette_game:
    build: .
    command: sh -c "sleep 4 && python manage.py run_roulette_game"
    env_file:
      - .env
    environment:
      DJANGO_DEBUG: false
      DB_NAME: casino
      DB_USER: casino_user
      DB_PASSWORD: casino_password
      DB_HOST: db
      DB_PORT: 5432
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      web:
        condition: service_started
    restart: on-failure

volumes:
  postgres_data:
